'use strict';

// =================================================
// SLIDESHOW Function
// -- all information came from: https://www.w3schools.com/w3css/w3css_slideshow.asp
// -- used code and just changed the ClassName
// =================================================
var slideIndex = 1;
showDivs(slideIndex);

function plusDivs(n) {
  showDivs(slideIndex += n);
}

function showDivs(n) {
  var i;
  var x = document.getElementsByClassName("locations");
  if (n > x.length) {slideIndex = 1}
  if (n < 1) {slideIndex = x.length} ;
  for (i = 0; i < x.length; i++) {
    x[i].style.display = "none";
  }
  x[slideIndex-1].style.display = "block";
}